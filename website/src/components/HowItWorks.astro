---
const steps = [
  {
    number: '01',
    title: 'Import Your JSON',
    description: 'Drag and drop a JSON file, paste from clipboard, or fetch from a URL. Pandia handles it all.',
    image: '/images/01_step.png'
  },
  {
    number: '02',
    title: 'Visualize Instantly',
    description: 'Watch as your JSON transforms into an interactive node diagram. Every object, array, and value gets its own node.',
    image: '/images/02_step.png'
  },
  {
    number: '03',
    title: 'Navigate & Explore',
    description: 'Pan, zoom, search, and click through your data structure. Understand complex nested objects in seconds.',
    image: '/images/03_step.png'
  }
];
---

<section class="relative py-32 bg-brand-darker overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute top-1/2 left-0 w-96 h-96 bg-brand-orange/10 rounded-full blur-3xl -translate-y-1/2"></div>
  <div class="absolute top-1/2 right-0 w-96 h-96 bg-brand-purple/10 rounded-full blur-3xl -translate-y-1/2"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-20">
      <span class="inline-block px-4 py-2 bg-brand-orange/10 text-brand-orange text-sm font-medium rounded-full mb-4">
        How It Works
      </span>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        From JSON to Diagram
        <br />
        <span class="gradient-text">in Seconds</span>
      </h2>
      <p class="text-xl text-brand-gray max-w-2xl mx-auto">
        Three simple steps to transform your JSON data into beautiful visualizations.
      </p>
    </div>

    <!-- Steps -->
    <div class="space-y-24">
      {steps.map((step, index) => (
        <div class={`flex flex-col ${index % 2 === 0 ? 'lg:flex-row' : 'lg:flex-row-reverse'} items-center gap-12`}>
          <!-- Content -->
          <div class="flex-1 max-w-xl">
            <div class="flex items-center gap-4 mb-4">
              <span class="text-6xl font-bold text-brand-gray-dark/50">{step.number}</span>
            </div>
            <h3 class="text-3xl font-bold text-white mb-4">{step.title}</h3>
            <p class="text-xl text-brand-gray leading-relaxed">{step.description}</p>
          </div>

          <!-- Image -->
          <div class="flex-1">
            <div class="gradient-border p-[2px] rounded-xl shadow-lg shadow-black/20 max-w-lg mx-auto lg:mx-0">
              <div class="bg-brand-dark rounded-[10px] overflow-hidden">
                <img
                  src={step.image}
                  alt={step.title}
                  class="w-full h-auto"
                />
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
